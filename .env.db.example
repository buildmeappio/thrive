# Local PostgreSQL (used by pnpm db:up, db:down, db:sync)
# Copy to .env.db or .env and adjust as needed.

PG_EXTERNAL_PORT=5441
PG_USER=postgres
PG_PASSWORD=postgres
PG_DB=thrive_db

# For db:sync only - remote DB via SSH bastion
# SYNC_DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
# SYNC_DB_PORT=5432
# SYNC_DB_NAME=thrive_db
# SYNC_DB_USER=dbadmin
# SYNC_DB_PASSWORD=your-password
# SYNC_BASTON_KEY=/path/to/key.pem
# SYNC_BASTON_HOST=bastion-ip-or-dns
# SYNC_BASTON_USER=ec2-user
# SSH_PORT=22
# LOCAL_PORT=15432
