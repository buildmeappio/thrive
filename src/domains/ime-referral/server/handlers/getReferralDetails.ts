const getReferralDetails = async (referralId: string) => {
  // const referral = await imeReferralService.getReferralDetails(referralId);
  return { success: true, result: referralId };
};
export default getReferralDetails;
