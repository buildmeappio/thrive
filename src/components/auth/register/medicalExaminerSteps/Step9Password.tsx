import React from 'react'
import type { MedExaminerRegStepProps } from '~/types'

export const Step9Password: React.FC<MedExaminerRegStepProps> = ({
  onNext,
  onPrevious,
  currentStep,
  totalSteps,
}) => {
  return (
    <div>
      Step 9
    </div>
  )
}

