{
  "name": "prisma-db",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "studio": "prisma generate && prisma studio",
    "reset": "prisma migrate reset",
    "seed": "prisma db seed",
    "docker:up": "docker-compose --env-file .env -f docker-compose.dev.yaml up -d",
    "docker:down": "docker-compose --env-file .env -f docker-compose.dev.yaml down -v",
    "db:sync": "chmod +x scripts/db-sync.sh && bash scripts/db-sync.sh",
    "migrate:timeslots": "tsx scripts/migrate-timeslots-to-utc.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thrive-org/prisma-db.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/thrive-org/prisma-db/issues"
  },
  "homepage": "https://github.com/thrive-org/prisma-db#readme",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.1",
    "prisma": "^6.15.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}